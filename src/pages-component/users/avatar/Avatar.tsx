import { Layout } from "~/layout";
import { AvatarContainer } from "~/pages-component/users/avatar/AvatarContainer";

export const Avatar = () => {
  return (
    <Layout title="avatar">
      <AvatarContainer />
    </Layout>
  );
};
